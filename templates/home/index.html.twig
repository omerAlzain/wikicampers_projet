{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .jumbotron-custom {
            background: url('{{ asset('images/home.jpg') }}') no-repeat center center;
            background-size: cover;
            height: 600px;
            color: white;
            margin-bottom: 0;
            display: flex;
            align-items: center;
            width: 100%;
        }
        .jumbotron-custom .container {
            max-width: 100%;
        }
        .btn-custom {
            background-color: #D4683E;
            border-color: #D4683E;
            color: white;
        }
        .btn-custom:hover {
            background-color: #B55232;
            border-color: #B55232;
        }
    </style>
{% endblock %}

{% block body %}
<div class="jumbotron jumbotron-fluid jumbotron-custom">
    <div class="container text-center">
        <h1 class="display-4">Bienvenue chez Wikicampers</h1>
        <p class="lead">Votre solution de location de véhicules. Découvrez nos offres et réservez dès aujourd'hui!</p>
        <a class="btn-custom btn-primary btn-lg" href="{{ path('vehicule_index') }}" role="button">Voir les véhicules</a>
    </div>
</div>

<div class="container mt-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">Large Sélection</h5>
                    <p class="card-text">Explorez notre large sélection de véhicules disponibles à la location.</p>
                    <a href="{{ path('vehicule_index') }}" class="btn btn-primary">Voir les véhicules</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">Tarifs Compétitifs</h5>
                    <p class="card-text">Profitez de tarifs compétitifs sur tous nos véhicules.</p>
                    <a href="{{ path('vehicule_index') }}" class="btn btn-primary">Voir les offres</a>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">Réservation Facile</h5>
                    <p class="card-text">Réservez facilement votre véhicule en ligne en quelques clics.</p>
                    <a href="{{ path('vehicule_index') }}" class="btn btn-primary">Réserver maintenant</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
